[package]
name = "axon"
version = "1.0.0"
edition = "2021"
description = "AXON — AI eXchange Optimized Notation"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
wasm-bindgen = "0.2"
